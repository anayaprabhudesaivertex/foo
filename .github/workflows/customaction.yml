name: Running custom action

on : push

jobs:
main:
  runs-on: ubuntu-latest
  steps:
  - name: checkout code
    uses: actions/checkout@v2
  - name: cal custom action
    uses: anayaprabhudesai/customrepo@main
